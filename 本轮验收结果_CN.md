# 本轮实际验收结果

日期：2026-09-17。

完整本地克隆复现流程退出码为 **0**。主源码保持800行/42个显式定理，未修改既有数学结论。

- 源码重编译：通过，`--trust=0`，警告作为错误。
- 同内核完整相关依赖重检：127个定理根、8,000项声明，42个显式定理全部包含；错误证明项和额外公理对照均被拒绝。
- 独立写出的目标类型文件：六个主命题逐一匹配。此文件由本项目编写，不是独立审稿人提供；仍使用同一Lean实现。
- 九种错误候选：全部按预期拒绝；另一个正确证明在存在不相关坏声明时仍被接受。
- 有限图辅助检查：16例、115,600个割。不是一般性证明或独立Lean检查器。
- 提交工具单元测试：19项通过。
- 新目录、本地Git固定版本、无本项目旧编译缓存，UID1000、网络系统调用禁用且拒绝探针成功，整套流程通过。

精确最终ZIP的哈希和封包后执行结果由压缩包旁的验收记录给出，避免“把自己的哈希写进自己”造成循环。

**未完成的事项没有冒充通过：**公开仓库尚待你创建和提供；本地Git克隆不是公开仓库复现。Nanoda/Comparator等独立检查器、官方安全版本、独立签署语义审查、首发优先权、题目范围认定和奖金均未确认。当前成果用于向官方发起审查PR，不是已批准的领奖文件。

使用 `提交步骤_CN.md` 公开工程、填仓库/分支/40位commit，再通过官方目录证据PR提交。旧包仅开Recipient Issue的说明已经废止。

全部机器可读结果及原始日志在 `evidence/current/`。
